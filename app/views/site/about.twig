{{ set(this, 'title', 'About') }}
{{ set(this, 'params', array.merge(this.params, {breadcrumbs: [this.title]})) }}

<div class="site-about">
  <h1>{{ this.title }}</h1>

  <p>This is the About page. You may modify the following file to customize its
    content:</p>

  <code>{{ this.viewFile }}</code>
</div>
